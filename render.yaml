services:
  - type: web
    name: telegram-video-bot
    env: python
    plan: free
    runtime: python-3.11
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
